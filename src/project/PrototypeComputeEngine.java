package project;

import project.annotations.ConceptualAPIPrototype;

public class PrototypeComputeEngine{
	@ConceptualAPIPrototype
	public void prototype(ComputeEngine engine) {
		// Reading and writing component
		// First initialize
		// From the user specified file input, read in the input from the file
		
		// Computation component
		// With the integer input, calculate the fibonacci sequence
		// to that given integer.
		// Then find all the even numbers and compute the sum.
		
		// Have the final output be available for the
		// data storage system.
		
		String result = engine.compute(0);
	}
}
